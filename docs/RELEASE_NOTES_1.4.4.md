# Agent Swarm 1.4.4

* **Chat:** Fixed server-mode OpenCode Chat through the workspace proxy, including API, SSE, WebSocket, assets, fonts, and workers.
* **Security:** Corrected OpenShell mTLS handling and enabled server certificate verification by default.
* **Stability:** Fixed GitHub App session refresh failures and expanded regression coverage.
* **Models:** OpenAI PLAN uses `gpt-5.6-terra` (`default`); BUILD uses `gpt-5.6-luna`.
* **Version:** Swarmer `1.4.4`; OpenShell `0.0.116`; OpenCode `0.4.1`.
