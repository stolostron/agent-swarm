fastapi==0.141.1
uvicorn[standard]==0.52.1
starlette==1.6.0
jinja2==3.1.6
python-multipart==0.0.32
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.22.1
argon2-cffi==25.1.0
cryptography==50.0.0
pydantic-settings==2.15.0
PyJWT==2.13.0
kubernetes==36.0.3
pyyaml==6.0.3
httpx==0.28.1
google-auth==2.56.3
websockets==17.0.1
itsdangerous==2.2.0
croniter==6.2.4
openshell>=0.0.72,<0.0.88     # OpenShell sandbox SDK, range-pinned:
                               #  - lower bound 0.0.72 is the first release whose
                               #    wheel ships real generated _proto/*.py files
                               #    (<0.0.72 ships only _proto/__init__.py, which
                               #    tests/conftest.py's real-proto pre-import needs).
                               #  - upper bound <0.0.88 excludes the release that
                               #    added a "workspace" field/kwarg to
                               #    SandboxClient.create()/.list(), which our code
                               #    doesn't pass and our deployed gateway (v0.0.82)
                               #    doesn't support. Bump past 0.0.87 only after
                               #    upgrading the OpenShell gateway and adopting the
                               #    Workspace API in openshell_client.py.
